import { DemoScheduleElement } from './demo-schedule-element.model';

describe('DemoScheduleElement', () => {
  it('should create an instance', () => {
    expect(new DemoScheduleElement()).toBeTruthy();
  });
});
